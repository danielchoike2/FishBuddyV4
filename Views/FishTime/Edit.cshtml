@model FishBuddy.Models.FishTime

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>FishTime</h4>
<hr />
<div class="row">
    <div class="col-md-4 bg-dark text-white">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FishTimeId" />
            <div class="form-group">
                <label asp-for="FishSpeciesId" class="control-label"></label>
                <select asp-for="FishSpeciesId" class="form-control" asp-items="ViewBag.FishSpeciesId"></select>
                <span asp-validation-for="FishSpeciesId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BestTimes" class="control-label"></label>
                <input asp-for="BestTimes" class="form-control" />
                <span asp-validation-for="BestTimes" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
